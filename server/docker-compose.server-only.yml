# Expose backend endpoints when the web reverse proxy is intentionally omitted.
services:
  api:
    ports:
      - "${ASTRA_API_BIND:-127.0.0.1}:${ASTRA_API_PORT:-8080}:8080"
  admin-console:
    ports:
      - "${ASTRA_ADMIN_BIND:-127.0.0.1}:${ASTRA_ADMIN_PORT:-8100}:8100"
